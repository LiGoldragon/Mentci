> Hunts criomev3 bit-rot per Li 2026-04-25: *"the stale code comes
> from v2 or v1 (first two approaches, now abandoned) ... let's flag
> the bit rot so we can get rid of it together."*

Sweeps the 20 sema-ecosystem repos under `~/git/`: criome, CriomOS,
CriomOS-emacs, CriomOS-home, horizon-rs, lojix, lojix-cli,
lojix-schema, lojix-store, nexus, nexus-cli, nexus-schema,
nexus-serde, nota, nota-serde, nota-serde-core, rsc, sema, signal,
tools-documentation. Out-of-scope sibling repos that share names
with retired components (e.g. `~/git/aski/`, `~/git/aski-core/`,
`~/git/askic/`, `~/git/aski-cc/`, `~/git/aski-core-bootstrap/`,
`~/git/aski-macro/`, `~/git/astro-aski/`, `~/git/corec/`,
`~/git/domainc/`, `~/git/nexus-spec-archive/`) are not visited —
those are the abandoned upstreams themselves and live alongside
the in-scope repos in `~/git/`. Pre-known and excluded:
`sema/reference/Vision.md` (just rewritten); the seven absorbed
nexus-schema modules (claimed deleted; **NOT actually deleted —
see §X cross-cutting**); `nexus/spec/{example,examples/...}.nexus`
(nexus/spec/ contains only `grammar.md` today; the promised
`{flow-graph,patterns-and-edits}.nexus` replacements were never
created — flagged in §X).

## Summary

| Repo | Tier-1 hits | Tier-2 hits | Tier-3 hits | Action |
|---|---|---|---|---|
| criome | 5 | 4 | 0 | minor (AGENTS.md heavy; src tiny) |
| CriomOS | 0 | 0 | 0 | clean (one historical aski mention in reports/0020 is a deletion log entry, not rot) |
| CriomOS-emacs | 18 | 0 | 0 | **heavy** — aski never removed despite CriomOS reports/0020 saying it was |
| CriomOS-home | 0 | 0 | 0 | clean (one comment marker for a past deletion is fine) |
| horizon-rs | 0 | 0 | 0 | clean |
| lojix | 0 | 0 | 0 | clean |
| lojix-cli | 2 | 0 | 0 | minor (one README aski sentence; one ARCHITECTURE.md sentence about `nexusd → nexus`) |
| lojix-schema | 0 | 0 | 0 | clean (only AGENTS.md on disk; no src to rot) |
| lojix-store | 4 | 0 | 0 | minor (4 mentci-next refs across AGENTS/Cargo.toml/lib.rs) |
| nexus | 32 | 2 | 0 | **heavy** — entire src/ + ARCHITECTURE.md still says nexusd |
| nexus-cli | 0 | 0 | 0 | clean (~10-line stubs only) |
| nexus-schema | 8 | 0 | 0 | **whole repo SHELVED 2026-04-25** — see §X cross-cutting |
| nexus-serde | 4 | 2 | 0 | minor (mentci-next + nexusd; field=value example; `<| |>` Tier-1 ref) |
| nota | 1 | 1 | 0 | minor (`:keyword syntax` claim is wrong; one criomed mention) |
| nota-serde | 0 | 0 | 0 | clean |
| nota-serde-core | 5 | 4 | 0 | minor (reports/013 Tier-1 ref in lexer.rs + tests/nexus_mode.rs; one nexusd link) |
| rsc | 0 | 0 | 0 | clean (anyhow style nit is in 080, not bit-rot) |
| sema | 2 | 0 | 0 | clean — Vision.md hits already-known; rest fine |
| signal | 23 | 0 | 0 | **heavy** — README + ~10 src files still claim "language IR lives in nexus-schema"; flow.rs is correct in passing |
| tools-documentation | 0 | 0 | 0 | clean (no v1/v2 baggage) |

Rough totals: **~104 Tier-1 hits, ~13 Tier-2 hits, 0 Tier-3 hits**
across the canonical 20 repos. The Tier-3 silence is good news —
no repo carries the pipeline-of-six-c's framing or "build DSL"
framing internally. (`reports/080 §3` already noted the
`Vision.md` corec/askicc/etc. occurrence; that file was rewritten
in this session and is no longer stale.)

## Per-tier findings

### Tier 1 — straight giveaways

#### `aski` (the retired name itself)

`/home/li/git/criome/AGENTS.md:38` — "`aski` is no longer in the
stack — its role as 'how types are specified' is now played by
`nexus-schema` records." **rewrite**: the rejection is correct
but the replacement is now wrong (nexus-schema is shelved as of
2026-04-25; types live in signal). Also drop the "earlier
framing" history paragraph (lines 41-46) that talks about
`criome-store`.

`/home/li/git/criome/ARCHITECTURE.md:578-579, 617` — Reject-loud
entries naming `aski-as-input` as a wrong frame. **keep** —
these are the canonical reject-loud entries Li explicitly wants
preserved (per §10 "Rejected framings" rule).

`/home/li/git/CriomOS/reports/0020-context-compact-prep.md:22-24,152`
— historical session log documenting that aski was *removed*
from CriomOS-home. **keep** — accurate history; not bit-rot.

`/home/li/git/CriomOS-emacs/flake.nix:28-30` — **live aski
dependencies**: `vscode-aski` flake input + `aski` flake input
pinned to `github:LiGoldragon/aski-core`. **delete** the three
inputs entirely; aski is retired and these still pull in real
remote sources at every flake update.

`/home/li/git/CriomOS-emacs/flake.lock` lines 3, 17, 23, 67, 71,
75, 79, 136, 255, 268, 403, 409, 415 — all locked references
following from the inputs above. **delete** by removing the
inputs and re-running `nix flake update`.

`/home/li/git/CriomOS-emacs/packages/mkEmacs/packages.el:11-12` —
`(use-package aski-ts-mode :mode ("\\.aski\\’" . aski-ts-mode))`.
**delete** the use-package block.

`/home/li/git/CriomOS-emacs/packages/mkEmacs/default.nix:8-10,
174-175, 238` — function arguments `tree-sitter-aski`,
`aski-mode`, `aski-ts-mode`, the `inherit aski-mode;` and
`inherit aski-ts-mode;` lines, and `tree-sitter-aski` in the
grammar list. **delete** all six lines.

`/home/li/git/CriomOS-home/modules/home/vscodium/vscodium/default.nix:39`
— `# All aski-related code dropped per Li 2026-04-25.` **keep**
(or delete the comment line, optionally — it's a tombstone, not
rot).

`/home/li/git/lojix-cli/README.md:28` — "`lojix` slot
('battery-included aski dialect' direction) moved to
`LiGoldragon/lojix-archive` on 2026-04-24." **rewrite or
delete**: the historical note is fine but mentions an
"aski dialect" framing that's now retired-twice-over. Either
trim to "the previous occupant of the `lojix` slot moved to
`lojix-archive` on 2026-04-24" or delete the §"Note on naming"
heading entirely.

#### `KindDecl` / `FieldSpec` / `TypeRef` (v2 schema-of-schema)

These are the schema-of-schema record kinds that `reports/078
§M0` explicitly **deferred post-MVP** in favour of flow-graphs
in `signal/src/flow.rs`. Per the corrections in `reports/078
§Open M0 corrections`: *"Don't scaffold schema-of-schema yet.
First milestone is flow-graphs; machina + KindDecl/FieldSpec/etc.
wait until the engine has been used to design them."* Anywhere
the docs/code still **assume KindDecl exists in sema** is rot.

`/home/li/git/criome/src/lib.rs:7` — module doc says step 1 is
"kind well-formed against `KindDecl`?" **rewrite**: should say
"kind name in `signal::flow::KNOWN_KINDS`?" for v0.0.1.

`/home/li/git/criome/src/validator/schema.rs:1, 8` — module doc
references `KindDecl` records in sema as the lookup target.
**rewrite**: validator schema-check pulls from
`signal::flow::KNOWN_KINDS` until machina lands.

`/home/li/git/criome/src/validator/cascade.rs:3` — "Stratified
per `KindDecl.stratum`". **rewrite**: cascade discipline needs
restating without leaning on a KindDecl field that does not
exist.

`/home/li/git/criome/ARCHITECTURE.md:100` — Invariant B
parenthetical: "(rkyv records described by KindDecl)".
**rewrite**: should say "(rkyv records described by built-in
Rust types in `signal::flow` for v0.0.1)".

`/home/li/git/nexus/src/client_msg/frame.rs:90` — test fixture
`nexus_text: "(Assert (KindDecl :name \"KindDecl\"))"`.
**rewrite**: change to a flow-graph fixture
`"(Node criomed [Sema engine])"` so the round-trip test exercises
the actual v0.0.1 substrate. (Doubly broken — also Tier 2 for
the `(Assert ...)` wrapper and the `:name` keyword and the
`"..."` quoted string; all four are wrong.)

`/home/li/git/nexus-schema/ARCHITECTURE.md:79` — "the signal
layer, kind names are wire strings; criome resolves them against
`KindDecl` at validation time." **delete the whole repo per Li
2026-04-25 'we should probably shelf it'** — see cross-cutting §X
(below).

`/home/li/git/nexus-schema/src/value.rs:6, 19` and
`/home/li/git/nexus-schema/src/edit.rs:64` — same. **delete**
(repo shelved).

`/home/li/git/signal/src/flow.rs:19, 61` — KindDecl mentions are
**correct in v3 framing**: they say "no `KindDecl`/`FieldSpec`
indirection — the Rust types ARE the schema for v0.0.1" and "no
in-sema `KindDecl` records needed for the first milestone." Both
explicitly negate KindDecl. **keep**.

`/home/li/git/signal/src/lib.rs:15` — module doc says signal
contains `KindDecl`/`FieldSpec`/`TypeRef` (schema-of-schema) AND
the absorbed nexus-schema modules. **rewrite**: signal does NOT
contain KindDecl today (M0 deferred); strike layer 3 from the
three-layer description, replace with "Sema record kinds (today:
the flow-graph kinds in `flow.rs`; future: KindDecl/FieldSpec/
machina once those land)."

`/home/li/git/signal/src/value.rs:6, 19` and
`/home/li/git/signal/src/edit.rs:64` — copy-paste bodies of
the deleted nexus-schema modules; reference KindDecl + TypeRef
as the validation target. **delete the seven absorbed modules**
(see cross-cutting §X) OR rewrite the doc-comments to say
"validation defers — schema-check uses `signal::flow::KNOWN_KINDS`
for v0.0.1."

`/home/li/git/signal/Cargo.toml:8` — package description
*"description = 'signal — the criome binary language. Wire
envelope (Frame, handshake) + language IR (RawPattern, RawOp,
AssertOp, …) + sema record kinds (KindDecl, Struct, Enum, …).
Anything criome is signal.'"* **rewrite** — remove the
"(KindDecl, Struct, Enum, …)" parenthetical; replace with
"(today: flow-graph Node/Edge/Graph; future: more as they land)".

`/home/li/git/sema/reference/Vision.md:102, 166` — already known
(file rewritten this session per the user's exclusion list).
**no action**.

#### `mentci-next` (repo was renamed to `mentci`; the GitHub URL has changed)

These are stale doc-link / dev-folder-name references. The local
working directory on Li's box is `~/git/mentci/` today; the
GitHub remote is `LiGoldragon/mentci`. Anywhere docs say
`mentci-next` they will 404 on github.com.

`/home/li/git/criome/AGENTS.md:17` — "`mentci-next/docs/architecture.md`".
**rewrite**: replace with `mentci/docs/...` (or just delete the
parenthetical — that doc is `criome/ARCHITECTURE.md` itself now).

`/home/li/git/nexus/src/lib.rs:12, 21` — two references in the
crate doc. **rewrite**: bulk `mentci-next` → `mentci` across the
nexus repo.

`/home/li/git/nexus/src/client_msg/mod.rs:40` — "live in
`mentci-next/reports/071`". **rewrite**: bulk replace.

`/home/li/git/nexus/src/client_msg/frame.rs:20` —
"`mentci-next/reports/071` §2.3". **rewrite**: bulk replace.

`/home/li/git/lojix-store/src/lib.rs:25` — "lojix scaffolding
(`mentci-next/reports/030` Phase C)". Both the URL and the
report exist no longer (current reports are 065/068/070/074/
076-080). **rewrite**: replace with current report number or
remove. Reports/030 was about an earlier sweep; 080 is the
current code-age audit; the reference predates the corpus
trim.

`/home/li/git/lojix-store/AGENTS.md:9, 33, 70` — three more
mentci-next references. Same prescription as above. AGENTS.md:9
points at "mentci-next/docs/architecture.md §5" which doesn't
exist in mentci/docs/ today (workspace-manifest.md and
dolt-workflows.md are the only docs); the §5 it cites has
moved into `criome/ARCHITECTURE.md §5`. **rewrite**.

`/home/li/git/lojix-store/Cargo.toml:9` — package-comment ref
to `mentci-next/reports/030`. **rewrite or delete**.

`/home/li/git/signal/README.md:7, 40, 66` — three full
github.com/LiGoldragon/mentci-next URLs. **rewrite**: bulk
`mentci-next` → `mentci` (URL works after rename — github
honours the redirect for now, but linkrot is on the clock).

`/home/li/git/signal/src/effect.rs:5,
pattern.rs:9, value.rs:10, request.rs:9, auth.rs:11,
edit.rs:8+10, frame.rs:12+55, query.rs:9, diagnostic.rs:12,
handshake.rs:13, reply.rs:7, slot.rs:7` — 14 module-doc
references to mentci-next. **rewrite**: bulk replace.

`/home/li/git/nexus-schema/src/{pattern.rs:9, edit.rs:8+10,
query.rs:9, diagnostic.rs:12, lib.rs:15, slot.rs:7, value.rs:10}`
— 8 references. **delete** (whole repo shelved — see §X).

`/home/li/git/nota/.beads/issues.jsonl:1` — one mentci-next
reference inside a bd issue payload. **review**: bd content is
historical; if Li wants the issue body refreshed, it's a single
field rewrite. Otherwise leave as is.

`/home/li/git/nota-serde-core/src/lexer.rs:11` — relative path
`../../../../../mentci-next/reports/013-nexus-syntax-proposal.md`.
**rewrite**: also Tier 2 — reports/013 proposed Tier-1 syntax
extensions (`<| |>` stream, `(|| ||)` optional pattern, `{|| ||}`
atomic txn) that are NOT in the locked v3 nexus grammar.md.

`/home/li/git/nexus-serde/src/lib.rs:20` — same `reports/013`
ref. **rewrite** (also Tier 2).

#### `nexusd` (renamed to `nexus` 2026-04-25)

`/home/li/git/nexus/README.md:5` — "Renamed from `nexusd`
2026-04-25". **keep** — this is the canonical rename note.

`/home/li/git/nexus/ARCHITECTURE.md:4-5, 84` — three more
"renamed from nexusd / local dir still nexusd/" notes. **keep
the architectural note; consider trimming the "local dir still
nexusd/" comment** since the local dir is `~/git/nexus/` today
(the dir was renamed at the same time per `~/git/` ls output).
Recheck: the local directory IS `~/git/nexus/` today. So the
"local dir still nexusd/" claim is stale-as-of-when-written and
needs **rewrite**.

`/home/li/git/nexus/src/main.rs:1` — module doc opens with
"nexusd — the nexus-text messenger." **rewrite**: change to
"nexus — the nexus-text messenger." Same verbiage that exists
in lib.rs.

`/home/li/git/nexus/src/lib.rs:1, 3, 6, 10, 15, 18` — six
`nexusd` mentions across the lib doc-comment. **rewrite**: bulk
nexusd→nexus.

`/home/li/git/nexus/flake.nix:28` — `name = "nexusd";` for the
devShell. **rewrite**: `name = "nexus";`.

`/home/li/git/nexus/src/client_msg/{mod.rs:1+11+17+19+39,
fallback.rs:1+10, reply.rs:1+21+34+41,
request.rs:1+9+13+16+24+32+39, frame.rs:1+9}` — **20 mentions**
across client_msg. **rewrite**: bulk nexusd→nexus across the
repo.

`/home/li/git/nexus/.beads/config.yaml:56` — "git+ssh://git@
github.com/LiGoldragon/nexusd.git" as the bd remote. **rewrite**:
update to nexus.git so bd sync resolves correctly. Same with
`.beads/metadata.json:5` (`"dolt_database": "nexusd"`) — the
dolt DB on dolthub may or may not have been renamed; verify
before changing.

`/home/li/git/nexus/spec/grammar.md:239` — "Consumer code (e.g.
[nexusd](https://github.com/LiGoldragon/nexusd), [nexus-cli]…)
derives serde…". **rewrite**: nexusd→nexus URL.

`/home/li/git/nexus-serde/README.md:17` — "[nexusd](https://github.com/LiGoldragon/nexusd) and [nexus-cli]". **rewrite**.

`/home/li/git/nexus-serde/src/lib.rs:24` — "(nexusd, nexus-cli)
which will define message types". **rewrite**.

`/home/li/git/nota-serde-core/ARCHITECTURE.md:11` —
"[nexusd](https://github.com/LiGoldragon/nexusd) — parses…".
**rewrite**.

`/home/li/git/lojix-cli/ARCHITECTURE.md:71` — "`nexusd → nexus`
happened and `criome → criome` will happen — `lojix` is the
family namespace". The "`criome → criome`" is also clearly a
typo / placeholder for "`criomed → criome`" but criome's binary
was never named criomed in current docs. **rewrite**: drop the
parenthetical or restate as "`nexusd → nexus` happened; the
`d`-suffix discipline applies to all daemon-name slots".

#### `lojixd` / `criomed` (the d-suffix forms; daemons dropped the d per Li 2026-04-25)

`/home/li/git/signal/src/flow.rs:1, 5, 16, 60` and
`/home/li/git/signal/src/lib.rs:59` — five `criomed` mentions in
the M0 substrate doc-comments. **rewrite**: bulk criomed→criome.
(These are otherwise correct v3 content; just the daemon name.)

`/home/li/git/nota/ARCHITECTURE.md:34` — "Edit / query semantics
(live in nexus + criomed)." **rewrite**: criomed→criome.

`lojixd` — **0 hits across the 20 in-scope repos**. Already
clean.

### Tier 2 — obsolete nexus syntax

#### `:keyword` field syntax (does not exist in v3 nexus)

The locked v3 grammar.md has no `:keyword` field-name token.
Records are positional only. The `:` character in nexus is the
**path separator** (`Char:Upper:A`, `Schema:Type:Field`) per the
nota grammar.

`/home/li/git/criome/ARCHITECTURE.md:336, 354, 382` — three
example nexus snippets:
- L336: `(Query (Fn :name :resolve_pattern))`
- L354: `(Mutate (Fn :slot 42 :body (Block …)))`
- L382: `(Compile (Opus :slot N))`
**rewrite**: convert all three to positional bare records. e.g.
`(Compile (Opus N))` if Opus has just a slot field; the others
need fictional positional shapes or replacement with flow-graph
examples.

`/home/li/git/nexus/src/client_msg/frame.rs:90` — already
covered in Tier 1 KindDecl section (`(Assert (KindDecl :name
\"KindDecl\"))`). Quadruple-broken — **rewrite**.

`/home/li/git/nota/ARCHITECTURE.md:23` — "(Pascal-named
records, `:keyword` syntax, `@bind` syntax…)" — **rewrite**:
nota does not have `:keyword` syntax; it has path syntax (`a:b`).
This sentence is description-error.

#### `(Assert ...)`, `(Mutate ...)`, etc. wrappers — bare records ARE assertions in v3

`/home/li/git/criome/ARCHITECTURE.md:336, 354, 377, 382` —
example snippets use `(Query …)`, `(Mutate …)`, "(Assert /
Mutate / Patch)", `(Compile …)` as outer wrappers. Per
grammar.md §"Messaging shapes": *"Assert — a plain record:
`(Point 3.0 4.0)` … Identical to nota. An assertion that the
record exists / is true."* and *"Mutate — `~` on a record or
pattern: `~(Point 0.0 0.0)`."* So Assert and Mutate are sigils,
not wrapper records. **rewrite** all four examples.

`/home/li/git/nexus/ARCHITECTURE.md:63` — "(Assert / Mutate /
Query / Subscribe / …)" in the signal table-cell. **rewrite or
keep**: this is naming the *signal Request enum variants* (which
are real Rust enum variants in `signal::Request`), not nexus
text wrappers — so it's actually correct as written. **keep**.

`/home/li/git/signal/README.md:18-19` — "Edit verbs (Assert /
Mutate / Retract / Patch / TxnBatch); query verbs (Query /
Subscribe / Unsubscribe); read-only Validate." **keep** — naming
signal::Request variants, not nexus syntax.

`/home/li/git/nexus/src/client_msg/frame.rs:90` — `(Assert
(KindDecl :name \"KindDecl\"))` — **rewrite** (already covered).

#### Quoted strings where bare-ident or `[...]` would work

`/home/li/git/nexus/src/client_msg/frame.rs:90` — `\"KindDecl\"`
inside the test-fixture. **rewrite** as part of the broader
fixture rebuild (Tier 1 above).

#### Tier-1 syntax extensions from the unlanded reports/013

These are NOT in the locked v3 grammar.md. The locked grammar
has 7 delimiter pairs (`( )`, `[ ]`, `[| |]`, `< >`, `(| |)`,
`{| |}`, `{ }`) and 4 sigils (`;;`, `#`, `~`, `@`, `!`,
plus `=` in narrow use; that's 6 sigils total per §10
"Sigil budget is closed"). The `<| |>` / `(|| ||)` / `{|| ||}`
forms from reports/013 add *three more* delimiter pairs that
the locked grammar does not include.

`/home/li/git/nota-serde-core/src/lexer.rs:11-12` — module
doc-comment claims the lexer recognises these three forms.
**review with Li**: are these forms part of v3 or not? The lexer
code probably implements them (tests/nexus_mode.rs exercises
them); the locked grammar.md does not. Either reports/013
extensions WERE adopted (and grammar.md needs updating) or
they were NOT (and lexer + tests need trimming).

`/home/li/git/nota-serde-core/tests/nexus_mode.rs:3, 95, 101,
109` — tests asserting these forms parse to specific Token
variants. **review** — same conditional as above.

`/home/li/git/nexus-serde/src/lib.rs:20-21` — module doc cites
the same `<| |>` / `(|| ||)` / `{|| ||}` forms with reports/013
URL. **review** — same.

`/home/li/git/nexus-serde/README.md:31` — example showing
`(Point horizontal=3.0 vertical=4.0)`. v3 records are positional,
not field=value. **rewrite**: should produce `(Point 3.0 4.0)`.
(Though the README's "shows what to_string outputs" framing may
mean the underlying serde currently produces field=value text;
if so, the **serde itself is broken** and should be fixed to
produce positional output to match grammar.md.)

### Tier 3 — retired pipeline framing

**Zero hits** across the 20 in-scope repos for the
"corec / askicc / askic / veric / domainc / semac" pipeline
listing. The `Vision.md` occurrence flagged in `reports/080
§"Suspect content"` was rewritten in this session; nothing
else carried forward. The reject-loud entries in
`criome/ARCHITECTURE.md` use `aski` as the framing-name (not
the pipeline-of-six-c's), which is correct anti-pattern flagging
not rot.

**Zero hits** for `Mentci/bridge/` — the directory never
materialised in v3.

**Zero hits** for `rsc — Rust projector. Mechanical
transformation from .sema to .rs` framing — `rsc/ARCHITECTURE.md`
correctly describes rsc as a sema → `.rs` projector but does not
use the "mechanical transformation" v2 phrasing.

**Zero hits** for `lojix — build DSL` framing. lojix-cli/README,
lojix/README, lojix/ARCHITECTURE all describe lojix as a daemon
or family-namespace; nothing calls it a DSL.

The `corec` token only matches in `CriomOS-emacs/flake.lock`
under nested input-following metadata for the (still-pinned)
aski-core dependency. Fixing the aski Tier-1 hits in
CriomOS-emacs auto-resolves this.

## Files recommended for outright deletion

### Whole-file deletions (no salvageable v3 content)

- **All seven absorbed nexus-schema modules in `signal/src/`**:
  `domain.rs`, `module.rs`, `names.rs`, `origin.rs`, `primitive.rs`,
  `program.rs`, `ty.rs`. **Reason**: per the user's exclusion list,
  these were "already deleted earlier this session." They are NOT.
  Per the v3 framing in `signal/src/flow.rs` and `criome/ARCHITECTURE.md`,
  the schema-of-schema (KindDecl/FieldSpec/etc.) AND the
  Rust-source records (Struct/Enum/etc.) are deferred until
  machina actually lands — both are out of scope for v0.0.1.
  Keeping these modules in signal makes signal carry ~7 modules
  of v2 leftover that the v3 design has explicitly rejected for
  M0. Deletion shrinks signal from ~21 modules to 14.

  Also delete the corresponding lines in `signal/src/lib.rs`:
  - Lines 49-57 (`pub mod` block for the seven modules)
  - Lines 86-98 (`pub use` block for their re-exports)

  Update `signal/Cargo.toml:8` to remove the
  `(KindDecl, Struct, Enum, …)` parenthetical from the
  description.

- **The whole `nexus-schema` repo** — already SHELVED per Li
  2026-04-25 ("we should probably shelf it"). The 14 source
  files in `~/git/nexus-schema/src/` are pure v2 baggage
  whose responsibilities now sit in `signal::flow` (for v0.0.1)
  or `signal::{value, edit, query, pattern, slot, diagnostic}`
  (for the language IR). The `mentci/devshell.nix` linkedRepos
  list already removed the auto-symlink per the recent commit.
  Two follow-ups outstanding:

  1. Delete `~/git/nexus-schema/` from local disk (the repo on
     github should be archived, not deleted, for jj-history
     reachability).
  2. The signal/src absorbed-modules deletion (above) closes
     the duplication.

  Until #1 happens, every file in nexus-schema/src/ is rot and
  every doc-link to nexus-schema is rot. Cleanest move: archive
  github repo + rm local clone + grep-and-fix any lingering
  README links that say "lives in nexus-schema".

### Block deletions inside otherwise-OK files

- **`criome/AGENTS.md` lines 16-46** — the entire "Stack" code
  block plus the "Earlier framing (historical)" paragraph plus
  the "aski is no longer in the stack" paragraph. The block is
  v1/v2-flavoured framing of the stack (criome runtime, sema,
  lojix-store, nexus, nota). The current canonical stack
  description lives in `criome/ARCHITECTURE.md §1` and this
  AGENTS.md text just drifts independently. **Delete the block**;
  replace with a one-line "see ARCHITECTURE.md" pointer. The
  rest of AGENTS.md (above and below this block) is fine.

- **`CriomOS-emacs/flake.nix` lines 28-30** — the three
  `vscode-aski` / `aski` flake inputs. **Delete**. Then `nix
  flake update` to clear the lock entries.

- **`CriomOS-emacs/packages/mkEmacs/packages.el` lines 11-12** —
  the aski-ts-mode use-package block. **Delete**.

- **`CriomOS-emacs/packages/mkEmacs/default.nix` lines 8-10,
  174-175, 238** — the aski function arguments + inherits +
  grammar list entry. **Delete**.

- **`lojix-cli/README.md` "Note on naming" §** (lines 23-30) —
  references "battery-included aski dialect" history. Either
  drop the section or trim to a one-line "renamed from `lojix`
  2026-04-25; previous occupant moved to `lojix-archive`."

## Files that need a rewrite (preserve good ideas)

### `criome/AGENTS.md`

- Heavy stale-stack-description block (lines 14-46). The
  *vision* paragraphs at top (1-13) and the *jj* note at bottom
  (57-60) are fine.
- **Salvage**: keep the criome-as-substrate framing (lines 1-13)
  + the Vision (lines 48-55). Drop the technical-stack table
  (lines 14-46) entirely; the canonical version is in
  `criome/ARCHITECTURE.md` and this file's drift is bit-rot.

### `criome/src/lib.rs`, `validator/schema.rs`, `validator/cascade.rs`

- All three reference `KindDecl` as the v0.0.1 schema-check
  target. The v3 design defers this — schema-check uses
  `signal::flow::KNOWN_KINDS`. **Rewrite the doc-comments** to
  reflect M0. Code bodies are `todo!()` so no logic change.
- **Salvage**: the six-step pipeline (schema-check → ref-check
  → invariant-check → permission-check → write → cascade) is
  correct architecture; only the schema-check doc-comment
  contents drift.

### `nexus/src/lib.rs`, `main.rs`, `client_msg/*.rs`,
### `flake.nix`, `ARCHITECTURE.md`, `.beads/config.yaml`,
### `.beads/metadata.json`

- ~32 `nexusd` mentions across the repo. **Bulk rewrite**
  `nexusd → nexus` (verify each — some are inside historical
  rename-notes that should stay).
- **Salvage**: the entire client_msg architecture is correct v3
  content; only the daemon-name token drifts.
- `flake.nix:28` devShell name fix is one line.
- `.beads/config.yaml:56` URL update + `metadata.json:5` rename
  need verification of dolthub state before flipping.

### `signal/README.md`, `Cargo.toml`, `src/lib.rs`,
### and ~12 src-module doc-comments

- ~14 `mentci-next` URL references + the Cargo.toml description's
  KindDecl/Struct/Enum mention + the lib.rs three-layer doc
  claiming layer 3 is in signal.
- **Bulk rewrite** mentci-next → mentci. Restate three-layer
  doc to "today: flow-graph kinds; future: machina". Revise
  Cargo.toml description.
- **Salvage**: the README's "what signal IS / IS NOT" framing
  is great; only the link-targets and the specific
  KindDecl/Struct/Enum naming drift.

### `nota/ARCHITECTURE.md`

- L23 says nota defines `:keyword` syntax — **wrong**. nota has
  path-separator `:` (`a:b`), not field-keyword `:foo`.
- L34 says criomed — should be criome.
- L35 references nexus-schema — wrong (shelved). Should reference
  signal (or just be deleted).
- **Salvage**: most of the file is correct; three line-fixes.

### `nota-serde-core/src/lexer.rs` + `tests/nexus_mode.rs`

- The Tier-1 syntax extensions (`<| |>`, `(|| ||)`, `{|| ||}`)
  exist in the lexer + tests but are NOT in the locked
  grammar.md. Either:
  - (A) v3 grammar.md is incomplete and needs adding these
    extensions (with §10 sigil/delimiter budget update) — the
    extensions would be useful for streams/optional/atomic-txn.
  - (B) extensions are NOT v3 and need removing from the lexer
    + tests + nexus-serde docs.
- **Decide first**, then sweep accordingly. Same situation in
  `nexus-serde/src/lib.rs:20-21`.

### `nexus-serde/README.md` + `src/lib.rs`

- README example produces `(Point horizontal=3.0 vertical=4.0)`
  which is not valid v3 (records are positional). Either the
  example is wrong (rewrite to `(Point 3.0 4.0)`) or the
  underlying serde code emits field=value format (which itself
  needs fixing to match grammar.md).
- **Verify with `nix run` or by reading `nota-serde-core/src/ser.rs`**
  whether the actual serializer emits positional or field=value.
  README needs updating to match reality after that.

### `lojix-store/AGENTS.md`, `Cargo.toml`, `src/lib.rs`

- 4 `mentci-next/reports/030` references. reports/030 doesn't
  exist anymore — the current reports are 065-080. Either point
  at a current report (080 for the most recent code-age audit)
  or just remove the citation. **rewrite**.

### `lojix-cli/ARCHITECTURE.md`

- Line 71 reference to "`criome → criome` will happen" is a
  typo (presumably `criomed → criome`, but criome's binary was
  never named criomed in current docs). The d-discipline
  parenthetical is fine in spirit but stated unclearly.
  **rewrite or delete the parenthetical**.

## Cross-cutting observations

### X.1 — Two pieces of the user's exclusion list are wrong

The user's prompt excluded:

1. *"`signal/src/{domain,module,names,origin,primitive,program,ty}.rs`
   — already deleted earlier this session."*
2. *"`nexus/spec/example.nexus` and
   `nexus/spec/examples/{flow-graph,edits,queries,genesis-seed}.nexus`
   — already deleted; replaced with
   `nexus/spec/examples/{flow-graph,patterns-and-edits}.nexus`
   in real positional grammar."*

For (1): the seven files **still exist** in `~/git/signal/src/`
and are still re-exported from `signal/src/lib.rs:51-57+86-98`.
The audit treats them as candidates for deletion since they
duplicate the (also-still-present) `nexus-schema/src/` modules
and lean on the v2 schema-of-schema framing. They appear in
`signal/Cargo.toml:8` package description.

For (2): `~/git/nexus/spec/` contains exactly one file —
`grammar.md`. Neither the deleted-set nor the replacement-set
exists. So the v3 grammar has no example files at all today.
**Recommend**: write the two replacement examples
(`flow-graph.nexus`, `patterns-and-edits.nexus`) so the locked
v3 grammar has at least one round-trippable corpus to demo +
test against.

### X.2 — `nexus-schema` SHELVED status is half-done

The repo IS marked SHELVED in `mentci/docs/workspace-manifest.md`
and IS removed from `mentci/devshell.nix` linkedRepos. But:

- The github remote still exists at
  `LiGoldragon/nexus-schema` (verify; archive if not already).
- The 14 source files at `~/git/nexus-schema/src/` are still
  intact and referenced from `~/git/signal/src/lib.rs` modules
  (which were *copied*, not *moved*, judging by the byte-for-
  byte similarity of doc-comments between
  `nexus-schema/src/value.rs` and `signal/src/value.rs`).
- The signal absorption sweep that the workspace-manifest
  promises is ~50% done: the modules were copied in but the
  originals weren't deleted, and signal's own Cargo.toml
  description hasn't been updated.

**Recommend**: complete the absorption — delete the seven
duplicate modules from `signal/src/` (they should never have
been imported; signal v0.0.1 should only carry the wire
envelope + language IR + flow.rs), archive the
nexus-schema github repo, delete the local clone.

### X.3 — `mentci-next` rename is a 25-file linkrot bomb

26 distinct file paths across 6 in-scope repos still say
`mentci-next` in URLs, paths, or AGENTS.md `Per X §Y` citations.
github currently honours the rename redirect, but:

- Every doc-link is one project-archive away from 404ing.
- Several paths point at reports that no longer exist
  (reports/013, reports/030, reports/037, reports/064,
  reports/070, reports/071, reports/074 — only 070 and 074
  survive the corpus trim per the current `mentci/reports/` dir
  listing).
- `lojix-store/AGENTS.md:9` cites "`mentci-next/docs/architecture.md
  §5`" — this doc no longer exists; the `§5` content was
  absorbed into `criome/ARCHITECTURE.md §5`.

**Recommend**: a single bulk sweep `mentci-next` → `mentci`
across all in-scope repos, paired with a verification pass
that each cited report actually exists in the post-trim
`mentci/reports/` dir.

### X.4 — Daemon-name discipline (`d`-suffix) is consistently broken in nexus repo

Per Li 2026-04-25 the daemon dropped the `d` everywhere:
nexusd → nexus. The repo, the docs, and the binary name all
moved. But:

- `nexus/src/main.rs:1` still says "nexusd — the nexus-text
  messenger."
- `nexus/src/lib.rs:1, 3, 6, 10, 15, 18` — six mentions.
- `nexus/src/client_msg/*.rs` — ~20 more mentions.
- `nexus/flake.nix:28` devShell name still `nexusd`.
- `nexus/ARCHITECTURE.md:4-5, 84` — though these are
  rename-history notes (some staying), the inline ones drift.
- `nexus/.beads/{config.yaml,metadata.json}` — bd integration
  points at a github URL and dolt DB still named `nexusd`.

The other repos (signal, lojix-cli, nexus-serde, nota-serde-core,
nota) carry isolated `nexusd` mentions that are easy 1-2-line
fixes. The **nexus repo itself** is the worst offender by
volume and the highest-impact rewrite.

### X.5 — `criomed` lingers in `signal/src/flow.rs` (M0 substrate)

The flow-graph M0 doc-comments use `criomed` five times. Per
the same daemon-rename discipline, this should be `criome`.
The substance is correct v3; only the name drifts.
`nota/ARCHITECTURE.md:34` is the only other in-scope criomed
hit — same fix.

### X.6 — Tier-1 syntax extensions live in lexer but not in grammar

The `<| |>` / `(|| ||)` / `{|| ||}` syntax extensions from the
unlanded `reports/013` exist in:
- `nota-serde-core/src/lexer.rs` (recognised tokens)
- `nota-serde-core/tests/nexus_mode.rs` (lexed in tests)
- `nexus-serde/src/lib.rs` doc-comment (cited as "deferred")

But are **NOT** in the locked v3 `nexus/spec/grammar.md` (which
caps the delimiter set at 7 pairs and the sigil set at 6).
This is the single biggest *grammar drift* in the codebase.

**Either**: (a) grammar.md gains these three pairs and updates
its sigil/delimiter budget, or (b) lexer/tests/nexus-serde docs
drop them. Per Li's "the syntax is locked" statement in the
recent commit, (b) seems likely but needs explicit confirmation.

### X.7 — bd / dolt remote references trail the renames

- `nota/.beads/issues.jsonl` — at least one issue payload mentions
  mentci-next inside the body.
- `nexus/.beads/config.yaml:56` — nexusd dolt remote URL.
- `nexus/.beads/metadata.json:5` — nexusd dolt DB name.
- mentci's own `.beads/config.yaml:56` (in this repo;
  `sync.remote: "git+https://doltremoteapi.dolthub.com/ligoldragon/mentci-next"`)
  also still says mentci-next per the search above.

**Recommend**: a separate sweep for bd/dolt config files since
sync correctness depends on these being accurate; the dolthub
DB names need verifying before flipping.

### X.8 — CriomOS-emacs ate the dark mass

The CriomOS reports/0020 documented "All `aski`-related code
dropped" but the cleanup only landed in CriomOS-home. The
CriomOS-emacs repo (created later as a planned replacement for
the legacy pkdjz/mkEmacs distribution) carried the aski
references along for the ride. **18 distinct aski hits** in
flake.nix + flake.lock + packages.el + default.nix. This is
the single largest cluster of Tier-1 rot in the audit, and
it's all in one repo.

**Recommend**: prioritise the CriomOS-emacs aski cleanup as the
single highest-leverage v1/v2 removal — one repo, ~6 files,
restores parity with the CriomOS / CriomOS-home pair.

---

## Triage prioritisation

Ranked by impact (which deletion clears the most cross-references):

### P0 — biggest cleanups, do first

1. **Complete the `nexus-schema` absorption** (X.2 above).
   Delete the seven duplicate modules in `signal/src/`; delete
   the `pub mod` + `pub use` blocks in `signal/src/lib.rs`;
   update `signal/Cargo.toml` description; archive the
   `nexus-schema` github repo; rm the local clone. This single
   change clears: 8 KindDecl Tier-1 hits in nexus-schema, ~14
   mentci-next references in nexus-schema modules, the entire
   ~14-module signal-vs-nexus-schema duplication, AND removes
   the v2 schema-of-schema framing from signal in one motion.

2. **Bulk `nexusd → nexus` sweep across the nexus repo**.
   ~32 occurrences. Single-file edits across src/main.rs,
   src/lib.rs, src/client_msg/*.rs, flake.nix, ARCHITECTURE.md.
   Verify .beads/* before flipping (dolthub DB rename status).

3. **Bulk `mentci-next → mentci` sweep across signal, nexus,
   lojix-store, nexus-serde, nota-serde-core, nota** (~26
   occurrences). Pair with verification that cited reports
   still exist in `mentci/reports/`. Strip references to
   reports/013, /030, /037, /064, /071 (no longer exist);
   point reports/070 / /074 references at the post-rename
   URLs.

### P1 — local but high-value

4. **Delete CriomOS-emacs aski code**. 18 hits across 6 files
   in one repo (X.8). Highest hit-density in the audit.

5. **Decide reports/013 Tier-1 extensions: in or out?** (X.6).
   If out: trim lexer + tests + nexus-serde docs. If in: update
   grammar.md to enumerate the three new delimiter pairs and
   bump the §10 sigil/delimiter budget. Either decision then
   propagates to ~5 file-level rewrites.

6. **Rewrite `criome/AGENTS.md` to drop the v1/v2 stack table**.
   Replace with a one-line pointer to ARCHITECTURE.md. Stops
   future drift between the two stack-description sources.

### P2 — low-volume but critical for fidelity

7. **Rewrite `criome/src/{lib.rs, validator/schema.rs,
   validator/cascade.rs}` doc-comments** — drop the KindDecl
   framing for v0.0.1; cite `signal::flow::KNOWN_KINDS`. Code
   bodies are `todo!()` so this is doc-only.

8. **Rewrite `nexus/src/client_msg/frame.rs:90` test fixture**
   to use a flow-graph example. The current fixture is wrong on
   four axes (KindDecl, `(Assert ...)` wrapper, `:name` keyword,
   quoted string) and is the only nexus-text example in
   compilable code.

9. **Rewrite `criome/ARCHITECTURE.md` example snippets at lines
   336, 354, 377, 382** — convert four `(Wrapper (… :keyword …))`
   forms to v3 positional bare records (or to flow-graph
   examples that match the M0 substrate).

10. **Bulk `criomed → criome` across `signal/src/flow.rs` +
    `nota/ARCHITECTURE.md:34`** (6 occurrences total).

### P3 — quality/cleanup

11. **`lojix-cli/README.md` "Note on naming"** — trim or delete.

12. **`nota/ARCHITECTURE.md:23`** — fix `:keyword` description.

13. **Write the two missing example files in `nexus/spec/`** —
    `flow-graph.nexus` and `patterns-and-edits.nexus`. Per the
    user's exclusion list, these were promised but never
    created. The locked v3 grammar has no concrete corpus
    today; round-trip tests can't run against grammar.md alone.

14. **`nexus-serde/README.md` example syntax** — verify whether
    the serializer emits positional or field=value, fix README
    or serializer to match.

15. **bd / dolt sync metadata** — verify dolthub DB names and
    update bd configs accordingly.

---

*End report.*
