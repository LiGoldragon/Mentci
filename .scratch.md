## 1

`?[Id, Title, Phase] := *thought{id: Id, title: Title, phase: Phase},` - why the
repitition (id -> Id)? I don't like repititions.

## 2

The main reason for bringing this up is because I already know `Saturnus` and
`Sol` are going to be enums themselves (there are different kinds of them, which
is useful data!)

## 3

Like I said, since you're generating the capnp programatically, you can query
for all the enums out of samskara
